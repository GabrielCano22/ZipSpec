services:
  backend:
    build:
      context: .
      target: backend
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build:
      context: .
      target: frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
